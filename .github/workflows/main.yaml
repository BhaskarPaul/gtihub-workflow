name: Shell Workflow

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: List directories
        run: ls
      - name: List all directories (-a)
        run: ls -a
